Starter AI Project

This tiny starter project shows a simple message.

Run (PowerShell):

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python starter_project\main.py        # prints message in console
python starter_project\main.py --gui  # shows a simple GUI message box (Windows)
```

No external packages are required.
